{
  "routes": [
    {
      "method": "GET",
      "path": "/invites",
      "handler": "invite.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/invites/count",
      "handler": "invite.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/invites/:id",
      "handler": "invite.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/invites",
      "handler": "invite.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/invites/:id",
      "handler": "invite.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/invites/:id",
      "handler": "invite.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
